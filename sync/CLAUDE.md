> ⚠️ 이 파일은 `C:\Projects\Tennis`(학습 PC)의 원본을 공개 저장소 `sync/`에 복사한 것(2026-09-26 01:49). 이메일 등 개인정보는 지웠음. **원본이 기준** — PC 세션에서 갱신하면 `deploy/sync-docs.ps1`로 다시 복사. 저장소에서 연 세션은 코드(app/)만 있고 research/·training-data/·deploy/tests/는 PC에만 있음.

# 테니스메이트 (TennisMate) — 프로젝트 지침

> 갱신: 2026-09-25 22:00 (앱 v1.42 = 앱으로 찍기 `mhRec*`(녹화하며 랠리 표시, 조각 IndexedDB `tm-mhrec` 5초마다·꺼져도 복구·지우기는 사용자만); v1.41 = 랠리 편집 화면 `mhEd*`·한 포인트 내보내기·프레임 단위 보기 `mhPv*`·점수판 `mhScoreCalc`·요약 카드 `mhSummaryShare`; v1.40 = 내 영상 하이라이트 서브 인식 `mhServes`·포인트 나누기·타수 `mhShots`·N타 이상 `mh-min`·공 따라 이동+확대(`mhPath` z, 새 형식 가로 확대); 경쟁 앱 TennisCut·PerfectSwing 조사(인수인계 2-53); v1.39 = 내 영상 하이라이트 녹화기 pause 제거·MP4 정리 안전장치·완성 줄 진단; v1.38 = 하이라이트 MP4를 보통 MP4로 다시 묶기 `mp4Defrag` — 카톡 0초; v1.37 = 멈춘 물체(네트 공·형광 쓰레받기) 제거 `mhDropStatic`; 9/25 v12·v13 미채택(v13 = 멈춘 점을 음성으로 학습 → 재현율 92.7→87.2) — 모델 v10 유지; v1.36 = 저장 공간 지킴·저장 실패 알림 — 9/25 폴드3 촬영분 전부 소실 사고; v1.35 저장소 확인; v1.34 = 저장 공간 자동 삭제 제거(경고만)·배터리 경고; v1.33 = 정밀 추적·챌린지에서 바운스 주변을 원래 해상도(최대 3배)로 다시 봐 착지점 다듬기 `aiNativeRefine`; v1.32 = 녹화 1080p 설정(안드로이드 기본)·비트레이트 8/5Mbps·하이라이트 1080p 출력; v1.31 = 하이라이트 MP4 — 카톡 공유; 9/24 새 코트 v11 미채택 — 모델 v10 유지; 앱 v1.30 = 같은 1분 두 번 저장 방지·안드로이드 캠 가로 고정; v1.29 = 캠 촬영 자동 복구(전화 뒤 녹화 재시작·페이지 재시작 시 자동 클럽캠)·하이라이트 검은 화면 대응; v1.28 = 내 영상 하이라이트: 동호인이 찍은 경기 영상 → 폰 안에서 랠리 찾기 → 세로/가로 하이라이트+원래 소리, 로그인 없이; v1.27 = 두 캠 판정 합치기 완성: 시계 맞춤·같은 바운스 판별·가까운 캠 우선·각 캠은 자기 쪽만 콜; v1.26 = 하이라이트 클립 재생 위치 수정·키프레임 1초·세로 OUT 라벨 규칙·공 계속 따라가기·착지 슬로모션; v1.25 = 앱판 세로 하이라이트 + 발리 가드; v1.24 = 소형 v10; 인수인계 2-33~2-36). 세션 시작 시 **`인수인계_다음세션.md`** → 이 파일 → `세션기록_*.md` 최신본 → 메모리 순서로 읽을 것.
> 사용자는 **한국어**로 소통한다.

## 1. 한 줄 요약
파트너 매칭 · 클럽 미니게임(KDK/청백전/단식대전) · 경기 기록/ELO 랭킹 · **AI 비디오 인아웃 판독**을 갖춘 한국어 테니스 웹앱.
제주 SBC 클럽에서 **클로즈드 베타 진행 중**. 최종 목표 = 심판 없이 폰 영상으로 자동 IN/OUT 판정.

## 2. 현재 상태 (2026-09-03)
| 항목 | 값 |
|---|---|
| 본체 | `app/index.html` **단일 HTML 앱 v0.73** (v0.72 = 뷰어 안 점수판 `cv-score`·리모컨 코드 고정 `tm-rccode`+경기 시작 시 자동 호스트·공기계 점수 표시 `ccScore`·하이라이트 클립 `ccKeepClip`(IndexedDB hl, 포인트마다 10초)+`ccBuildHighlight`(캔버스 재인코딩 → 저장+본폰 전송); v0.73 = 클럽 랭킹 기간(누적/이번 달/지난 달)+월간 시상 카드+시상 PNG 공유 `clubAwardImage`; v0.69~71 = **리디자인**: 탭 5개 홈·클럽·판독·랭킹·더보기(`data-tab`, `showTab` btn 생략 가능), `#tab-judge`(판독 센터+클럽캠)·`#tab-more`, 헤더 한 줄+코트 라인, 이모지→SVG/제거(정적 234곳 + 런타임 `uiTidy` MutationObserver, 상태 이모지 🟢🔴🟡✂⛶만 유지), 설명문 `details.help`, 빈 섹션 `data-hide-empty`, 클럽캠 설정 `#cc-settings` 접기, 코트 모드 `#modal-live` 다크 고대비. **규칙: 새 UI에 이모지 쓰지 말 것(제목·버튼·라벨) — 런타임이 지움**; v0.68 = 🧪 학습 샘플 자동 수집 `clSampleUpload`; v0.67 = 노애드 기본 `live.noAd`(듀스 후 한 점, TB는 7점·2점 차); v0.66 = 점수 콜 테니스식 러브/피프틴/써티/포티·올·서버 먼저, 워치 동일; v0.65 = 공기계 2대 동시 시청 `cvWatchB`·판정 합치기(가까운 캠 우선, `cy`)·클립 듀얼 뷰어 페어링; v0.64 = 라이브 샷 정보 스핀·속도·코스·깊이 `clShotInfo`, 참고치·실코트 튜닝 예정; v0.63 = 점수 콜 소리·음성 입력·서브/폴트/렛; 이전 v0.53 (인라인 CSS/JS, 빌드 도구 없음). v0.50 사파리 mp4 폴백(`recPick`) · v0.51 iOS 카메라 자동 재획득(`ccReacquire`) · v0.52 캠 진단 줄(`cc-diag`)+프레임없음 복구 · v0.53 ⛶ 전체 화면 캠 모드(`ccFullToggle`, 스테이지를 body로 이동) · v0.54 캠 코드 폰별 고정(`tm-cccode`)+뷰어 코드 기억(`tm-cvcode`)·무프레임 안내 · v0.55 보정 기준 corners/service(`clCalMode`, 가까운 서비스라인 y=18.285)+화각 선택 · v0.56 화각 기본 16:9 복귀(4:3은 iOS 16에서 검은 화면 → 옵션+자동 폴백). **규칙: iOS 카메라 제약은 새 값을 기본으로 넣지 말고 옵션으로** · v0.57 🔄 새로고침 복구(`ccHardReload`/`ccMaybeAutoReload`, 재시작 후 자동 ccToggle). · **v0.58 원인 해결**: iOS는 `getUserMedia`를 탭 직후 컨텍스트에서만 허용 → 요청을 핸들러 첫 줄(`camReq`)로, 거부 시 `cc-tap` 큰 탭 버튼. **규칙: iOS 카메라/마이크 요청은 어떤 await보다 먼저, 자동 복구는 탭 유도로. 음성 합성(TTS)도 같은 탭 안에서 한 번 speak()해야 이후 채널 이벤트로 말함(v1.13 `ccTtsUnlock`, 9/17 — 체크박스를 껐다 켜면 되던 증상)** · v0.59 `<video>`가 rs0(트랙 live인데 0×0)이면 `ccBindVideo`로 다시 붙임(제스처 불필요, 3회) — **규칙: iOS video에 srcObject는 요소가 보이는 상태에서, muted/playsInline은 프로퍼티로** · v0.60 🗑️ 캠 영상 지우기(`ccClearAll`) · v0.61 화각 선택 제거(16:9 고정)+`<video>` rs0 지속 시 캔버스 대체 미리보기(`ccPreviewStart`, `cc-preview`) · v0.62 캔버스 미리보기 **항상** 사용(본폰 뷰어는 되는데 아이폰만 검음 = video 컴포지팅만 죽음). **규칙: iOS PWA에서 카메라 미리보기는 `<video>` 직접 표시 대신 캔버스 drawImage로** · **9/4 결론: 아이폰 8 Plus 클럽캠은 홈 화면 앱(PWA)이 아니라 사파리 브라우저에서 사용**(WebKit bug 252465 — PWA에서 getUserMedia 스트림 재생 불가, 갤럭시 캠은 정상) |
| 공개 URL | https://yjc20170201-sudo.github.io/tennismate/ (GitHub Pages, PWA 설치형) |
| 배포 | 루트 `배포하기.bat` → `deploy/deploy.ps1` (app/ → `pages-repo/` 복사 → commit → push, version.json 갱신) |
| 백엔드 | Supabase **서울(ap-northeast-2)**, 프로젝트 `tennismate` ref `ssibxgplavavsqemqrej` |
| 마이그레이션 | `backend/migrations/001~010` 전부 적용됨. **다음은 011부터** (007 = 보안 강화, 008 = storage `train/` 경로 허용, 009 = club_boards 진행 중 대진표 실시간 공유, **010 = 관리자 모드: `admins`(본인 uid 등록)·`visits`(접속 기록, 하루 한 행)·`is_admin()`·`touch_visit()`·`admin_users()`(이메일·최근 로그인·접속 통계·클럽, security definer)·`admin_visits(days)`**) |
| 학습 데이터 파이프라인 | 통영상: **v0.96 📥 캠 영상 전부 PC로 받기(WebRTC, PC 크롬 뷰어에서 폴더 선택, 같은 와이파이; v0.97 캠이 나중에 켜져도 자동 재시도, **v0.98 캠 폰 [PC로 영상 보내기 대기] = 카메라·녹화 없이 채널만 열어 응답 + 캠 코드 항상 표시** — 옮기기 전엔 클럽캠 시작을 누르지 말 것. **v1.10: 폰 보관 상한 설정 `tm-cc-keep`(2/4/8/16시간, 기본 4시간=240개) — v1.09까지는 120개(2시간) 고정이라 더 찍으면 옛 영상부터 삭제됐음(9/14 사용자 자료 유실)**)** 또는 공기계 내보내기(묶음 5/10/20/전부, v0.95) → 사진첩 → **USB + 루트 `아이폰_영상_가져오기.bat`**(DCIM 전 폴더에서 raw/에 없는 영상만 `raw/<오늘>_아이폰/`로) 또는 구글 드라이브 → PC. 학습 조각: 클럽캠이 바운스마다 `clips/train/<코드>/<ts>/` 에 6장+meta.json 자동 업로드(v0.68, 하루 400팩) → 루트 `학습샘플_가져오기.bat` → `training-data/samples/`. 학습은 이 PC(RTX 3080 Ti)에서 세션 중 실행 |
| 베타 | 이메일 인증 OFF, SBC 클럽 시드됨(한경면), 가입 7명 |
| 워치 리모컨 | `app/r.html` (초경량, 코드 입력 → 큰 버튼 2개). **2026-09-04 개편**: 세트/게임/포인트 큰 표시, 점수판 탭 = 음성 읽기, 포인트 후 자동 음성(ko-KR speechSynthesis, 토글 `tm-rc-voice`), 게임·세트·종료 진동 패턴. 워치4 클래식 삼성 인터넷 북마크 동기화로 열기(타이핑 불필요) |
| 본폰 | **갤럭시 Z 폴드3**(WebGPU 가능 — 판독 센터 WASB·실시간 모델 1차 테스트 기기) |
| 공기계(클럽캠) | **iPhone 8 Plus** (A11, iOS 16까지). 1080p60 촬영 가능. **스윙비전 설치 불가**(iOS 18·A12 이상 요구, 안드로이드판 없음). 사파리라 녹화는 mp4(v0.50 `recPick`), rVFC·WakeLock은 iOS 16.4+ OK. **2026-09-04 실기기 확인: PWA 설치·로그인·클럽캠 라이브 썸네일을 본폰에서 보기까지 정상** (✂️ 클립·보정·라이브 판정은 미확인) |

### 폴더 지도
```
app/            본체(index.html, r.html, sw.js, manifest, tracknet.js + tn-weights.bin, wasb.js + wasb_tennis.onnx(MIT, licenses/))
pages-repo/     GitHub Pages 공개 저장소(yjc20170201-sudo/tennismate) — 배포 산출물만
backend/        migrations/001~006 SQL, supabase-접속정보.txt(URL·anon key만)
deploy/         deploy.ps1(배포), publish-site.ps1, github-connect.ps1
supabase/       Supabase CLI 설정(config push용)
tracknet/       TrackNet 원본(가중치 추출 스크립트) — 브라우저 이식 완료
research/       공 탐지 모델 로컬 평가+학습 — 평가 전용, ext/ 코드는 앱 반입 금지. .venv(uv, torch cu124), ext/ 클론, weights/, results/, pipeline/(라벨→검수→데이터셋→학습 WASB 파인튜닝·tiny 증류→측정→ONNX), RUNBOOK.md
training-data/  raw/날짜_라벨/ — 공 탐지 자체 학습용 영상 · 편집/<날짜>/ — 경기 편집본(루트 경기_편집.bat, research/pipeline/match_edit.py: 라벨로 랠리만 잘라 세션별 mp4+포인트_목록.csv+하이라이트) · 편집/<날짜>/세로_하이라이트/ — **쇼츠용 세로 하이라이트(루트 세로_하이라이트.bat, research/pipeline/highlight_vertical.py, 9/21; **9/23: webm seek 실패·프레임 수 메타 대응 `seek()`, 옆 코트 공 제거 `court_band`, 발리(코트 밖 2.5m↑ 투영) 제외·OUT 라벨은 가까운 코트만**): 9:16 창이 공을 따라가고(영점 지연 이동평균) 공 꼬리·바운스 고리·구질/속도/깊이 라벨(앱 aiSpin·clShotInfo 규칙 이식, 속도는 가까운 코트만)·미니 코트 낙하점·확실한 OUT. 코트 보정은 앱 자동 보정(court_check.mjs)을 초기값으로 twocam/calib.py가 다듬음(--init 수동 가능, 보정_확인.jpg 저장). 9/18 상위 20랠리 7.8분 생성. fps 메타가 깨진 파일은 패킷 시각으로 실측(`real_fps`, 폴드3 webm은 메타 1000·실제 29fps — 예전처럼 60으로 간주하면 2배속), 출력 fps는 소수점 그대로. 9/23: 폴더 하나로 돌리면 출력이 `세로_하이라이트_<라벨>`(같은 날 두 캠이 서로 덮어쓰던 문제), `--title`은 제목 타일에 표시**
클럽캠/         미니캠 SD카드 수집 도구(캠영상_가져오기.bat, 사용법.md), probe 스크립트
tennis-app/     구 Next.js 리그 앱 — 라인 판정 기술 자산(호모그래피·스코어링)으로 보존. 폐기 금지, 개발 안 함
claude/         구 인수인계 문서(08-30, v0.38 기준 — 참고용, 일부 구식)
```

## 3. 핵심 기능 (앱에 구현된 것)
- **계정/프로필**: 이메일 로그인(TMAuth), 온보딩, "둘러보기" 데모 모드. `TM_CONFIG` 주입됨 = 서버 모드 ON.
- **랭킹**: 서버 ELO(전원 1000 시작, K=32), **단식/복식 분리**(004), 양쪽 승인 후 반영. 제주 14구역 정복 지도.
- **클럽**: clubs/club_members(005). **v0.94 진행 중 미니게임 실시간 공유**: 호스트가 대진·점수를 넣으면 `club_boards`(009)에 upsert → 회원은 클럽 탭 맨 위 "지금 진행 중인 경기"에서 읽기 전용으로(Realtime+30초 폴링). 토글 `board-share`. 미니게임 KDK 개인전(5~12명)·청백전·단식 대전 → `kdk_events` 저장(006). **클럽 누적 포인트 랭킹은 종목 분리(v0.48)**: 복식 탭 = 개인전+청백전 합산, 단식 탭 = 단식 대전만 (`clubRankKind`, 종목은 note 문자열로 판별 `kdkEvKind` — 저장 note 패턴 바꾸면 깨짐). 랭킹은 `results` 배열에서만 집계(참가 +10·승당 +10) — 특정 인원을 순위에서만 빼려면 `results`에서 행 제거(2026-09-03 도희 사례, names/games는 보존). 실명 통일 옵션(`nameMap`, localStorage `tm-namecfg`).
- **라이브 스코어**: 세트/게임 설정, **v1.03 선수 1 선택(`lv-p1`, `live.p1Id` — 내가 아니면 내 기록·ELO 미반영, 두 팀 로컬 레이팅만)**, 점수 리모컨(Realtime `score:코드`), 랠리 촬영 모드(링버퍼, 점수 탭=직전 랠리 클립). **v0.63**: 🔊 콜 소리(`lvSpeak` — 본폰 TTS + 캠 채널 `say`로 공기계 `ccSay`가 외침, 토글 `tm-lv-voice`/`tm-cc-voice`), 🎤 음성 입력(`lvMicToggle`, SpeechRecognition ko-KR: 우리/상대/폴트/더블/렛/취소, TTS 중 무시), 서브 추적 `live.srv`(게임마다 교대), 폴트/더블폴트(리시버 포인트 자동)/렛. 워치 `r.html`도 음성.
- **비디오 판독 센터**: 코트 자동 보정(aiDetectCourt, 합성 오차 2.6px·실코트 미검증) · 공 추적 v3(프레임 차분+트랙 연결) · TrackNet 정밀 추적(tfjs) · 바운스 보간(aiRefineBounce) · IN/OUT 판정(**v0.47: 라인 폭 5cm + 공 반지름 반영, 단식 모드 토글 `aiSingles`** — 보정점은 라인 중심 기준) · 클립 전체 스캔 · 스핀 분류 · MoveNet 폼 분석/코트 커버리지 · 설치 가이드 오버레이.
- **📡 클럽캠 v0.45**: **v1.25 앱판 세로 하이라이트(9/23): 라이브 판정 결과(공 위치·바운스·판정)를 포인트 클립에 동봉(`clHlLog`·`ccHlPack`)하고 [하이라이트 만들기]에서 세로(쇼츠) 체크(`tm-cc-hl-vert`, 기본 켬)면 540×960 창이 공을 따라가며 꼬리·착지 고리·IN/OUT 라벨·미니 코트 낙하점(`ccHlVertFrame`). 라이브 판정 켜져 있어야 함. 같은 판에서 `clDetect` 공중 타구 가드(코트 밖 2.5m↑ = 바운스 아님), 먼 코트 자동 아웃 60cm↑. **v1.26(9/23 점검): 포인트 클립 = 구간 첫 조각 + 마지막 10초인데 재생기가 빈 구간을 원래 시각대로 재생해 13초 제한 안에 실제 장면이 한 번도 안 나왔음(v0.72부터, 헤드리스 실측) → 저장 때 남긴 부분 시작 초 `s0`를 기록하고 만들 때 `s0+1`초로 이동(`ccHlStart`), 영상 0초 기준 `ccRecT0`. 녹화기 `videoKeyFrameIntervalDuration: 1000`(폴드3 크롬 기본 키프레임 3.4초라 조각을 빼면 이동 직후 멈춤; 아이폰 사파리는 원래 1초). 세로 OUT 라벨은 자동 콜과 같은 규칙 `ccHlCallLabel`(아슬아슬 제외, 먼 코트 60cm↑·가까운 코트 15cm↑, 아니면 라벨 없이 노란 점), 판정 틱마다 트랙 점 기록 `clHlTrackLog`(사이드라인 ±1.5m 띠 `clHlBand` 밖 = 옆 코트 제외)로 바운스 없이도 따라감, 판정 붙은 착지 앞뒤 0.4배 슬로모션 `ccHlSlowAt`. 테스트 `deploy/tests/hlclip_test.mjs`(실제 녹화·이어 붙이기·만들기, 그려진 장면 시각 검증).** **v1.27 두 캠 판정 합치기 완성(9/23 — 스윙비전과의 차별점: 폰 한 대는 먼 코트가 구조적으로 약함): 뷰어에서 [B 추가]로 두 캠이 연결되면 `cvSyncStart`가 10초마다 두 캠에 `ping`(캠이 `pong`으로 자기 시계 응답 → `cvClock` 시계 차, 왕복 짧은 표본 우선)과 `dual`(짝 알림, 35초 안 오면 캠이 스스로 해제)을 보냄. 캠은 짝이면 `clDualNearOk` — 자기 보정 좌표 y ≥ L/2(가까운 절반)만 자동 콜. 판정 메시지에 `bounceAt`·`cx`·`near` 추가. 뷰어 `cvCall`: 착지 시각을 뷰어 시계로(`cvBounceT`) 0.5초 안(시계 못 맞췄으면 0.9초) + 같은 자리 3m 안(B 좌표는 코트 뒤집어 비교 `cvToA`)이면 같은 바운스(`cvDualSame`), 가까운 캠 우선(`cvDualPrimary`), 판정이 다르면 기존 경고 표시, 먼 캠만 본 바운스는 "먼 쪽 캠만 봄" 참고 표시, 자동 콜 음성 1.5초 안 중복 안 함, 챌린지는 마지막 바운스를 본 캠에만(`cvChallengeSlot`). 전제: 두 캠 모두 코트 보정(가까운 베이스라인 = y L). 테스트 `deploy/tests/dualcam_test.mjs`. 실기기 미검증.**  **v1.29 촬영 자동 복구(9/24 — 전화 뒤 4개만 찍히고 멈춘 사고): 가려지는 순간 조각 저장(`hidden-save`), `ccRecWatch` 녹화 지킴이(8초 조각 없음 → 녹화 재시작 → 카메라 재획득), `tm-cc-running`(sessionStorage)으로 페이지가 꺼졌다 열려도 `ccAutoRestart`(안드로이드 탭 없이), 복구되면 음성. 규칙: 3m에 단 캠은 사람이 못 누른다 — 끊김 대응은 전부 자동+소리로. 하이라이트 녹화 캔버스는 화면에 붙이고(`#mh-live`/`#cc-hl-live`) 원본 video는 보이는 썸네일로 — 안드로이드 크롬 검은 프레임 대응, 검으면 VideoFrame 전환. 테스트 `ccwatch_test.mjs`.** **v1.30: `ccSealSeg` 한 번에 하나+저장 조각 비움(같은 1분 두 파일 중복 방지), 안드로이드 캠 시작 때 전체 화면+가로 고정 `ccLandscapeLock`(자동 회전 꺼져도 가로 영상).** **v1.32 녹화 화질 `tm-cc-res`: 1080p 8Mbps(안드로이드 기본)/720p 5Mbps(아이폰 기본, 예전 2.5Mbps) + ~~저장 한도 90% 넘으면 추가 삭제~~ → v1.34에서 제거(옮기기 전 영상 보호, 경고만). 규칙: 사용자 촬영분은 사용자가 정한 보관 상한 말고는 앱이 스스로 지우지 않는다. v1.34 배터리 경고·8% 이하 즉시 저장.** 공기계 폰 = 실시간 캠(`cc*`), 내 폰 뷰어(`cv*`), ✂️ 직전 20초 → 판독센터. **v1.06 절전 녹화 모드**(`ccEco*`, localStorage `tm-cc-eco`): 야외 직사광 발열 대응 — 가림막 `cc-eco-shade`로 화면 검게, 미리보기 그리기·라이브 판정 정지, 썸네일 6초마다, 녹화·중계·WakeLock 유지, 탭하면 5초 보기. 캠 종료 원인 이력 `track-ended`(열 차단 의심)/`hidden`/`pagehide`. **v1.15 촬영 지킴이 `ccGuardTick`(1.5초마다): ① 영상이 세로(videoWidth<videoHeight)면 경고, 페이지가 가로가 되면 `ccReacquire('orient')` 자동 — 9/20 폴드3가 자동 회전 꺼진 채 가로로 걸려 39분이 옆으로 누운 720×1280·26fps로 저장됨 ② 3분 무터치면 자동 절전(`tm-cc-eco-auto`, 기본 켬, 라이브 판정 중엔 안 함, 설정 저장 안 함). 9/20 야외 직사광: 8 Plus 27분·폴드3 39분 만에 발열로 꺼짐.** **v1.16 캠 끊김 알림 `ccDownAlert`: 전화·알림 등으로 카메라가 끊겨 탭이 필요해지면 캠 폰이 말로 알림(20초마다 최대 6번, 콜 소리 설정과 무관) + 뷰어에 `camdown`/`camup`(`cvCamDown`/`cvCamUp`, 본폰 표시+음성), 시작 때 방해 금지 안내, hidden→visible 끊긴 초 이력. 규칙: 캠 폰은 진동 금지(거치 각도가 바뀜 — 사용자 9/20) — `ccShowTap`의 진동 제거.** 본폰 한 대로 찍는 사용자는 전화가 오면 끊김(아이폰은 통화 앱으로 넘어가면 네이티브 앱이어도 끊김, 안드로이드 네이티브는 포그라운드 서비스로 유지 가능) → 방해 금지 안내가 기본 대책. **v1.17 렌즈 선택·줌(클럽캠 설정): `ccLensRefresh`(카메라가 열린 뒤 enumerateDevices로 뒷면 렌즈 목록, `tm-cc-lens`)·`ccCamConstraints`는 저장된 선택이 있을 때만 `deviceId exact`(기본은 예전 그대로 facingMode 자동 — 카메라 제약 기본값 불변 규칙), 고른 렌즈가 안 열리면 저장을 지우고 자동 복귀. 줌은 트랙 `zoom` 능력이 있는 기기만 슬라이더(`ccZoomSetup`/`ccZoomApply`, `tm-cc-zoom`, 최대 4배). 배경: 9/20 폴드3가 초광각으로 잡혀 코트가 작고 휘게 찍힘.** **v1.20 렌즈 자동 선택(9/22): 카메라가 열린 뒤 뒷면 렌즈가 2개↑이고 사용자 선택이 없으면 `ccLensAuto`가 안드로이드 라벨 "camera2 N" 중 N 최소(삼성 0 = 기본 광각)로 `ccReacquire('lens')`(세션당 1회, `ccLensSessionId`, 저장 안 함). 보정 뒤 `clLensCheck`: 역산 초점거리 f/폭 < 0.62면 초광각 → `#cl-lens` 빨간 띠 + [다음 렌즈로 바꾸기](`ccLensNext`). 테스트 `lensauto_test.mjs`. 9/22 폴드3 영상도 초광각(f≈550px)이었음 — 라벨 공 검출률 1~8%.** **v1.21 전자동(9/22 저녁): 캠 시작 3초 뒤 `clAutoSchedule`이 자동 보정을 스스로 시도(실패 시 15초마다 최대 20회), 성공하면 `clAutoAfterCal` — 초광각이면 버튼 없이 `ccLensNext` 자동(최대 2회, 재획득 뒤 재스케줄), 아니면 높이 결과를 음성으로("설치 높이 좋아요 약 3.6미터"/"카메라가 낮아요 3.1미터 이상으로"). 사용자가 3m에 달아 놓으면 화면을 못 보고 못 누르기 때문. `clAutoCalib(silent)`는 성공 여부 반환. 테스트 `autocal_test.mjs`.** **v1.23 초광각 되돌리기(9/22 19:07): 사용자가 설정에서 고른 렌즈(`ccLensPref`)가 있으면 자동 전환 안 함; 자동으로 광각으로 바꾼 뒤 보정이 3회 연속 실패면 이전 렌즈로 되돌리고 세션 동안 초광각 허용(`clUltraAllowed`, 이력 `lens-revert`, 음성 안내). 규칙: 코트가 다 보이는 것이 화각보다 우선 — 폴드3 광각은 가까운 베이스라인이 안 들어옴.** **v1.18 설치 높이 자동 점검(9/22): 캠 보정이 정해질 때마다(자동·수동·복원) `clHeightShow` → `clHeightCheck(clH, vw, vh)`가 호모그래피에서 카메라 위치를 역산(`clCamPose`: 주점=화면 중심 가정, f²=(|h2xy|²−|h1xy|²)/(h1z²−h2z²), C=−Rᵀt; 8 Plus 720p 실측 f≈1040px=이론 1044)해 높이·베이스라인 뒤 거리·"먼 코트의 네트 쪽 몇 %가 그물에 가리는지"를 `#cl-height` 색 띠로 표시. 기준: 먼 서비스라인이 네트 윗단(0.914m) 위로 보이는 높이 = 0.914·(Yc−ys)/(N−ys) (뒤 3m면 3.05m) 이상 = 좋음, 먼 베이스라인만 보이면 보통, 아니면 낮음. 추정이 말이 안 되면(초점거리·위치 범위 밖, 두 식 불일치) 아무 말도 안 함. 테스트 `deploy/tests/height_test.mjs`(합성 카메라 4종 + 9/14 실보정값 → 2.5m). 배경: 지금까지 거치 높이 2~2.5m라 먼 코트 대부분이 네트 그물 뒤 — 먼 바운스를 못 잡던 주원인.** **8 Plus는 9/13 야외에서 1분 만에 꺼짐(원인 미확정)**. 영상은 공기계 IndexedDB에 축적(v0.47 최근 120분 → v1.10 설정 2~16시간·기본 4시간, 초과 시 옛것부터 자동 삭제·저장 실패 시 상태 표시), 💾 전체 저장으로 학습데이터화.
- **🤖 클럽캠 라이브 판정 v0.49 (준실시간)**: **v1.07 학습 모델 경로** — 설정 `cl-model`(`tm-cl-model`, 기본 = `navigator.gpu` 있는 폰만 켬)이면 `clModelStart`가 wasb.js+소형 v4를 열고 `clModelTick`이 rVFC 틱에서 3장을 512×288 캔버스에 모아 `WASB.detectFrames`(topk 3, 임계 0.4) → `clFrames`에 후보 → `clModelTrack` = `aiTrackCands(frames, tw, 4)`(연결 반경 상한 폭 48%) → `clDetect`(모델 경로는 바운스 인덱스 원시 최대점). 추론 중엔 틱 건너뜀 → 초당 7~10장 표본(폴드3 WebGPU ≈0.3초/3장 예상), 900ms/3장 넘게 5회면 프레임 차분으로 자동 전환. 헤드리스(wasm 257ms/3장)에서 실코트 클립 3회 연속 IN 362~369cm(판독 센터 366cm). WASB 원본은 라이브에 안 씀. 캠 폰 모듈 `cl*` — 카메라 스트림 프레임을 되감기 없이 직접 받아(`requestVideoFrameCallback`, ≤28fps, 320px 축소) 3프레임 차분 후보 → 1.6초 창 `aiBuildTracks` → `aiBounceIdx`/`aiRefineBounce` → 캠 자체 보정 `clH`(✨자동 `aiDetectCourt` / 📐수동 4점 탭, localStorage `tm-clcal`에 해상도와 함께 저장·복원)로 `aiVerdict` → 채널 `call` 이벤트 → 뷰어 `cvCall`이 판정 피드(`cc-call-last`/`cc-call-feed`) 표시. **아슬아슬(5cm 이내)이면 자동 ✂️ 20초 클립**(토글 `cl-autocut`, 25초 간격). 캠 오버레이 캔버스 `cc-cam-overlay`. 합성 검증만 — **실코트·실기기 미검증**. `aiBuildTracks` 첫 연결 반경 15→30px(빠른 공 트랙 시작 실패 수정, 사후 분석에도 적용).
- **🎬 내 영상 하이라이트 v1.28(9/24, `mh*`)** (**v1.31: 결과물은 `recPickShare` MP4(H.264+AAC) — 카톡은 WebM을 거부. 공유용 결과물은 항상 MP4** · **v1.38: 크롬 MP4는 조각 MP4라 머리 길이 0 → `mp4Defrag`로 보통 MP4(카톡 0:00 방지, 실패·데이터 부족이면 원본) · v1.39: 녹화기 pause/resume 금지(폰 크롬 MP4가 멈춘 뒤 끊김 의심) — 이동 중엔 전환 화면, 완성 줄 진단 · v1.37: `mhDropStatic` 한자리에 오래 있는 것 제거. 규칙: 멈춘 물체(네트 공·쓰레받기·놓인 공)는 공이 아니다 — 추적·하이라이트는 0.5초 뒤에도 같은 자리면 버린다**(v1.40f: 3초 안 같은 자리 2번↑ 또는 전체 6번↑만 — 토스는 남김) · **v1.41: 분석 → 편집 화면(목록) → 고른 랠리로 만들기(두 단계) · v1.42: 앱으로 찍기(찍는 동안 표본 수집, 조각은 5초마다 IndexedDB — 촬영분 보호 규칙 동일)**): 홈 첫 카드 `#home-mh` → 판독 탭 맨 위 `#mh-sec`. 폰 영상 여러 개 → 소리 없는 분석 요소 `mhVid`로 배속 재생(rVFC 연속 3장 → 소형 v10 → `mhPickBall` 움직이는 공만) → `mhRallies`(3초 간격 잇기, 좁은 움직임 3.5초↑면 자르기, 3초↑) → 긴 랠리 N개/전부 → 만들기 요소 `mhRVid`(원래 소리 `createMediaElementSource` → 녹화) 세로 720×1280(공 따라가기 `mhPath`)/가로 → 저장·공유. 코트 보정되면 옆 코트 띠 `mhBandFor(H, vh)`(공중 공 위해 아래로 25% 허용). **규칙: 소리 노드를 만든 video는 그 노드를 어딘가(소리 0 게인→destination)에 이어 두지 않으면 재생이 멈춘다.** 판정·미니 코트·타수는 넣지 않음(길이 초만). 테스트 `deploy/tests/myhl_test.mjs`(+ dump/seg 조정 도구). 업로드 없음, 로그인 없이 됨.
- **홈 바로가기(v0.47)**: 홈 상단 카드에서 🎮 미니게임(`kdk-sec`)·📹 판독 센터(`vj-sec`)·📡 클럽캠으로 바로 이동(`goSection`). 1인 게이미피케이션 12섹션은 기본 숨김(`HOME_SECTIONS.off`, ⚙️ 홈 편집에서 켤 수 있음).
- **코트 정보**: 제주 실코트 12곳 딥링크(psf.kr/전화/카카오맵). **자체 예약은 받지 않는다**(공공 API 조회 전용 — 되돌리지 말 것).

## 4. 확정된 결정 (되돌리지 말 것)
- 프론트는 **단일 index.html 유지**. 빌드 도구는 Phase 3(RN 포팅)과 함께.
- **클라이언트를 믿지 않는다** — ELO·랭킹·권한은 전부 서버(RPC/RLS) 검증. 레거시 ELO는 서버 미반영.
- 베타 지역 = **제주** 1곳 집중. 예약 정면 승부 회피, 차별점은 매칭·랭킹·기록·판독.
- Supabase 도메인 HTML 호스팅 불가(text/plain 강제) → 우회 시도 금지. 배포는 GitHub Pages만.
- 하드웨어: **미니캠(A9) 뚫기·IP캠 모두 보류**. 자동 인아웃은 **폰 1080p 60fps**가 기준. 미니캠은 SD카드 인아웃 증거용.
- **줌아웃은 소프트웨어로 불가**(2026-09-04). 코트가 안 들어오면: 4:3 화각(앱) → 서비스라인 기준 보정(앱) → 더 뒤·더 높게 거치 → 클립형 광각 렌즈 0.6x. iPhone 8 Plus는 초광각 없음.
- 탐지 모델: TrackNet V2~V4(대만 NCTU/NYCU 계열) 라이선스 없음 → **앱에 코드·가중치 반입 불가**. 단, **로컬 평가는 진행**(2026-09-04 사용자 결정: 상업 목적 아닌 "되는지 확인"이므로 이 PC에서만 돌려보고 배운 뒤 자체 구현). 규칙: ① 클론은 `research/` 아래에만, `app/`·`pages-repo/`에는 절대 금지 ② 논문·구조·전처리 아이디어는 자유롭게 반영, 코드·가중치 복사 금지 ③ 학습 데이터는 우리 코트 영상 + 라이선스 확인된 공개셋만 ④ `research/README.md`에 "평가 전용, 배포 금지" 명시. 목표 = V2~V4 중 우리 코트에서 제일 잘 잡히는 구조를 골라 자체 학습(RTX 3080 Ti) → 웹 포팅. **2026-09-07 평가 결과**: `research/` 환경 구축(uv venv, torch cu124), WASB-SBDT(NTT, **MIT**)·TrackNetV3(qaz812345, **MIT**)는 라이선스 있음 → 반입 자체는 가능하나 학습 데이터셋 라이선스 미확인이라 우리 데이터 재학습 권장(사용자 결정 대기). WASB 테니스 가중치가 실코트 60초에서 바운스 7개(6 실제) vs 앱 라이브 3개. `research/results/2026-09-07/README.md`.
- **정복 지도·대회 탭은 유지**(2026-09-03 사용자 결정: 최종 목표가 지역 대회 주최). 베타에서 숨긴 건 실제 연동 없는 가짜 기능뿐(레슨/코치, 데모 예약·양도·빈자리, 잠긴 AI 코치) — 코드는 보존, `hidden` 블록.
- **Realtime 채널**: `cam:`·`room:`은 private(로그인 필수, 007 정책). `score:`는 워치 리모컨(r.html)이 익명이라 공개 유지 + 코드 6자리. 애플워치 네이티브(Phase 3) 전까지는 이 타협.
- **실전 모드(로그인)에서는 데모 배열 전부 비움**(`enterRealMode`) — 새 데모 데이터를 추가하면 여기에도 반드시 등록.

## 5. 작업 규칙
- **index.html 패치**: `.mjs` 스크립트로 exact-anchor 교체 + 고유성 검사. 통째 덮어쓰기 금지.
- **버전 올리기**: `APP_VERSION = 'v0.xx'` 갱신 → 헤드리스 스모크 → 배포(`deploy/deploy.ps1`) → **`curl`로 라이브 `APP_VERSION` 확인한 뒤에야 "배포됨"이라고 보고**(2026-09-06 v0.72·73 배포 누락 사고). 시작 시 `APP_VERSION`이 이 문서·세션기록과 다르면 덮어쓰지 말고 보고.
- **헤드리스 테스트**: **playwright-core는 `deploy/tests/node_modules`에 영구 설치(9/20 — `%TEMP%\pwtest`는 임시 폴더 정리로 두 번 날아감), 테스트는 `node deploy/tests/<이름>.mjs`**. (옛 기록: `%TEMP%\pwtest`의 playwright-core + chromium), `page.evaluate`로 전역 함수 호출(ME·players는 모듈 스코프). 테스트 훅 `window.__svTestClient`, `__rcTestClient`, `__poseTestDetector`.
- **인증 흐름(v1.22, 9/22, 사용자가 직접 배포 aa739f3)**: `flowType: implicit`(재설정·가입 확인 링크를 다른 브라우저에서 열어도 됨), `redirectTo = origin+pathname`, `PASSWORD_RECOVERY` → `openNewPw` 모달 → `TMAuth.updatePassword`. 다른 회원 비밀번호를 서버에서 직접 바꾸는 일은 하지 않는다(대시보드 Auth › Users 또는 본인 재설정으로 안내). 자동 모드에서 인증·비밀번호 관련 SQL 적용과 배포가 차단될 수 있음 → 사용자가 `배포하기.bat`/SQL Editor로 직접.
- **관리자 모드(v1.19, 9/22)**: 더보기 › 설정 › "관리자" 줄은 `is_admin()`이 참인 계정에만 보임(`adminTouch`가 로그인 뒤·화면 복귀 때 호출, 같은 자리에서 `touch_visit` 10분에 1회). 모달 `#modal-admin`: 가입자 표(닉네임·이메일·가입·최근 로그인·접속일수/횟수·마지막 접속·NTRP·지역·클럽)+최근 접속 기록. 이메일은 개인정보 — 화면 밖으로 내보내지 말 것. 관리자 추가는 `insert into admins(user_id)`. 테스트 `deploy/tests/admin_test.mjs`(가짜 rpc).
- **SQL**: 새 마이그레이션은 `backend/migrations/011_*.sql`부터. `npx.cmd --yes supabase@latest db query --file <경로> --linked --project-ref ssibxgplavavsqemqrej`로 원격 실행(PowerShell에서 `npx.cmd` 필수). `realtime.messages`에 `alter table`은 권한 없음 — 정책만 만들 것.
- **패치 스크립트 보관**: `deploy/patch-v0xx.mjs` (예: `patch-v047.mjs`). 한 번만 실행, 재실행 시 앵커 없음으로 중단됨.
- **사용자 입력을 innerHTML에 넣을 땐 `esc()`** (v0.47 추가). 현재 4곳만 적용(게시글 제목/작성자, 파트너 카드 이름/지역/스타일) — 나머지 innerHTML 137곳은 데모 상수 위주지만 서버 닉네임이 흐르는 곳은 발견 즉시 감쌀 것.
- **GPU 배치 추론은 서로 다른 입력으로 검증하고 실제 프레임에서도 한 번 대조**(2026-09-09 v1.01 — 폴드3 WebGPU가 같은 입력 검증은 통과시키고 실제 배치는 첫 묶음 복제). 배치 관련 수정 뒤엔 `%TEMP%\pwtest\batch_guard_test.mjs`.
- **자동 업데이트(v1.05)**: `version.json`에 `app` 버전이 실리고(deploy.ps1이 index.html에서 추출), 페이지의 `APP_VERSION`과 다르면 녹화·전송·경기 중이 아닐 때 한 번 자동 새로고침(`?u=시각`으로 HTTP 캐시 우회), 그래도 옛것이면 바 안내. 8 Plus 사파리가 옛 페이지에 갇히던 원인 = 옛 방식은 build 문자열의 변화만 봐서 캐시된 옛 페이지를 못 알아챔. 테스트 `%TEMP%\pwtest\update_test.mjs`.
- **앱이 받는 외부 파일(스크립트·ONNX 모델·가중치)은 전부 `?v=APP_VERSION`으로 로드** — index.html만 새것이고 스크립트(v0.88)·모델(v0.91)은 HTTP 캐시 구버전이던 사고. 파일을 바꿨는데 주소가 같으면 폰은 옛것을 쓴다.
- **날짜는 `todayStr()`(로컬)** — `toISOString().slice(0,10)` 금지(UTC라 새벽 0~9시에 전날).
- **bat 파일은 반드시 CRLF**, 한글 든 .ps1은 **UTF-8 BOM**.
- **시크릿**: anon key만 앱/문서에. service_role·카카오 시크릿·DB 비번은 절대 파일·대화에 쓰지 않는다. 노출 시 재발급 권고.
- **촬영분 보호(v1.36, 9/25 폴드3 몇 시간 촬영분 소실 사고)**: 저장 공간이 차면 지우지도 덮어쓰지도 말고 녹화를 멈추고 소리로 알린다(`ccSpaceOk` 한도 80%). 저장 실패는 절대 조용히 넘기지 않는다(재시도 + `ccDownAlert`). 크롬은 디스크가 가득 찬 상태의 쓰기 실패를 IndexedDB 손상으로 보고 통째로 지울 수 있다. 녹화 해상도·비트레이트 기본값을 올릴 땐 한도가 차는 시간을 계산할 것(1080p 6Mbps ≈ 45MB/분 ≈ 10GB/3.7시간).
- **사용자 본인이 하는 것**: 계정 가입/로그인/인증코드/비밀번호/결제/프로젝트 삭제/공개 저장소 생성. 대신 하지 않는다.
- git 저장소가 아닌 루트(`C:\Projects\Tennis`)는 되돌릴 수단이 없다. 삭제·덮어쓰기 전 원본 확인.
- 파일 전달: 사용자가 claude.ai에서 다운로드 → `Downloads` → 내가 복사.
- 이 프로젝트는 주식 자동매매(`C:\Users\snim2\Desktop\26년 7월 자동매매시작파일`, `C:\TradingDev`)와 **완전히 분리**. 참조도 하지 않는다.

## 6. 스윙비전 대비 체크리스트 (기능 기준표, 2026-09-03)
스윙비전은 코드·모델 비공개 상용 앱 → **가져다 쓰기 불가, 기능을 우리 방식으로 재구현**. 아래를 하나씩 지워간다.
전부의 전제는 **공 탐지** — 이게 안 되면 통계는 전부 허수. 공 탐지가 되면 속도·낙하 위치는 코트 보정+바운스 보간에서 거의 공짜.

| 스윙비전 기능 | 상태 | 우리 앱 | 남은 것 |
|---|---|---|---|
| 폰 1대 코트 자동 인식 | 🟡 | **v1.09 모양 검사 강화**(corners 폴백 폭 비율 0.08~0.32 + 네트 y 일치 거부 + 위·아래변 기울기 차 5° 거부 — 네트를 먼 베이스라인으로 잡던 것·펜스 상단 쓰레기 차단). **v1.08 야외 수정**: 9/13 야외 코트 C에서 네트 띠를 서비스라인으로 잡아 코트가 절반으로 눌리고 판정 거리가 2배 나던 문제(`accuracy_score` 55드롭 + `court_check.mjs` 8프레임으로 발견) → 사이드라인 바깥까지 이어지는 밝은 줄(네트)을 찾아 그 아래에서만 서비스라인을 고름 + `aiCalSane` 모양 검사(위/아래 폭 비율 0.35~0.98, corners 0.2~)로 엉뚱한 결과는 실패 처리(→ 수동 보정 안내). 코트 A(실내)·C(야외) 프레임 투영 라인 일치. **지붕 코트 B는 서비스라인 마스크 지지율 0.19라 아직 실패**(어둡고 얇은 라인 → 마스크 개선 과제). 실패 사유 `aiDetectCourtSvc.why`. **v0.77 aiDetectCourtSvc(베이스라인·서비스라인·사이드라인 기준)** — 실코트 7프레임 성공, 투영 라인 일치. 기존 네 귀퉁이 방식은 실코트 실패(먼 베이스라인 네트에 가림·귀퉁이 화면 밖) | 코트 B 마스크, 실기기 확인, 클레이 |
| 공 추적 | 🟡 | **v1.24 기본 모델 = 소형 v10(9/23): v7 → v9(9/22 아이폰 낮, 카메라 3m↑) → v10(9/22 야간 다른 코트 아이폰 120·폴드3 114) 통산 64k×10에폭(GPU OOM 2회로 3번에 나눠 학습). 검증 11개 @0.4 clean: 재현율 90.0%·정밀도 91.8%·바운스 105/123 vs v7 87.5/91.4/105 — 처음 보는 야간 코트 재현율 70→88·82→89, 높은 카메라 정밀도 87→95, 기존 코트 동급(야외 C 바운스 27→23 주의). `aiModelThr` 정규식을 두 자리 버전(v10)도 0.4가 되게 수정. `research/results/2026-09-23/README.md`. 교훈: 같은 코트를 더 넣으면 제자리(v7·v8), 새 코트·새 조명·높은 카메라를 넣으면 바로 오른다.** **v1.12 기본 모델 = 소형 v7(v5에서 이어 9/16 실내 경기 181개 추가, 64k×10에폭 5시간; 검증 7개 @0.4 clean 라벨: 재현율 89.8%·정밀도 91.8%·바운스 78/89 vs v5 89.3/91.2/78 — 동급, 9/16 코트 정밀도 +3.4%p; `research/results/2026-09-17/README.md`. v6(궤적 정리 라벨만)은 미채택. **WASB 의사 라벨 기준 포화** — 다음 이득은 손 정답(data/gt)뿐)**. **v1.11 기본 모델 = 소형 v5(v4에서 이어 9/14 실내 경기 59개 추가, 64k×10에폭 290분; 검증 6개 @0.4: 재현율 93.3%·정밀도 91.9%·바운스 72/77 vs v4 93.7/87.5/73 — 9/14 영상 정밀도 64.9→85.3%; `research/results/2026-09-16/README.md`. 라이브 판정도 v5)**. **라벨 품질 도구(9/15)**: 궤적 정리 `pipeline/clean_labels.py`(→ `data/labels_clean`, 오탐 966 제거·보간 14,063), 손 정답 클릭 도구 루트 `라벨_클릭도구.bat`(→ `data/gt` 부분 파일), `common.load_labels_merged` = gt > clean > raw(build_dataset `--raw`, eval `--labels raw|clean`) — v6부터 clean 적용. **v1.04 기본 모델 = 소형 v4(v3에서 이어 9/13 경기 130개 추가, 48k×12에폭, 검증 영상 5개(9/13 2개 포함) 임계 0.4: 재현율 93.1%·정밀도 92.7%·바운스 67/71 vs v3 93.2/84.6/55; `research/results/2026-09-13/README.md`. train.py best(4px f1)와 eval(10px) 기준이 어긋나 v4는 last.pt 채택)**. **v1.02 기본 모델 = 소형 v3(v2에서 이어 32k×16에폭, 임계 0.4: 검증 재현율 96.0%·정밀도 89.5%·바운스 25/27; `research/results/2026-09-10/README.md`)**. **v1.01 wasb.js 배치 검증 강화(서로 다른 입력 2묶음 + 실제 프레임 첫 호출 대조) — 폴드3 WebGPU가 12묶음에서 첫 묶음을 복제하던 버그로 v2가 못 찾던 원인 해결**. **v1.00 정밀 추적 실패 시 모델·백엔드·프레임 수를 상태 줄에 표시(진단용)**. **v0.99 기본 모델 = 소형 v2(9/9 경기 2시간 46,688프레임 추가 학습, 임계 0.4: 검증 재현율 94.7%·정밀도 89.4%·바운스 25/27 vs v1 90.1/89.1/22)**. v0.93 공이 화면 밖으로 나가면 "화면 밖" 배지+마지막 위치 판정+음성**. v0.92 wasb.js가 로드 시 GPU 출력·배치 출력을 wasm 기준과 자가 검증(틀리면 자동 폴백)**. v0.90 기본 모델 = 우리 소형 v1(폴드3 webgpu 11초, WASB 26.5초, 판정 동일), 12장 배치 추론**. v0.89 바운스가 정지 지점 앞이면 뒤로 최대 1.6초 자동 연장, wasm 강제 옵션**. v0.87 판독 센터 모델 선택: WASB(5MB) / 우리 소형 v1(0.8MB, 5.6배 빠름, 앱 규격 동일)**. v0.86 정밀 추적 프레임 캡처 = 재생(rVFC), seek 폴백**. v0.83 공이 날아가는 중이면 최대 +3.3초 자동 연장 + [2초 더 보기] 이어서 분석**. v0.82 먼 코트 확대 2차 추론(바운스 없거나 위쪽일 때)**. v0.81 놓인 공 무시(정적 후보 제거)+후보 3개 움직임 연결+바운스 평활**. v0.80 정밀 추적: 자동 보정+트랙 정리+구간 자동 연장+IN/OUT 음성**. v0.79 판독 센터 정밀 추적 = WASB-SBDT 테니스 모델(NTT, MIT, ONNX 5MB, onnxruntime-web webgpu→webgl→wasm)**, TrackNet은 폴백. 자체 추적 v3. **실코트(2026-09-07): 320px는 공 2px라 사람만 추적(실제 0회) → v0.77 640px+사람 군집 제거+이동거리 트랙 선택: 오판정 27→1/분, 재현율 ~30%** | 재현율(사람 마스크·다중 트랙·60fps) → 자체 학습 |
| 자동 인아웃 판정 | 🟡 | 바운스 보간 + 판정 로직(라인 폭·단식 반영) + 클럽캠 라이브 판정(v0.49) + **v1.27 두 캠 합치기(각 캠은 가까운 절반만 판정·콜, 시계 맞춤)** + **v0.84 라이브 자동 콜(확실한 아웃 15cm↑·서브 첫 바운스 서비스 박스 밖 폴트만 소리, 토글 cl-autocall)** | 공 탐지 품질, 실코트 검증, 서비스 박스 판정, 공 중심→지면 투영 보정 |
| 실시간 처리 | 🟡 | 웹앱 준실시간(캠 폰 스트림 직접 처리) + **v1.07 라이브 판정에 소형 v4 모델(WebGPU 폰 기본, 초당 7~10장 표본)** | 스윙비전급 실시간·자동 점수는 네이티브 앱(Phase 3) |
| 챌린지(되감기 리뷰) | ✅ | 판독 센터, 클럽캠 ✂️ 20초, **v1.14 [챌린지] = 캠이 마지막 바운스 앞 2.5초·뒤 2초만 올리고(`ccPickChunks`, `clLast.bounceAt`, `fromEnd` 힌트) 뷰어가 받자마자 바운스 시각으로 이동해 정밀 추적 자동 실행(`aiChallengeRun`) → 결과 카드 `aiResultCard`(바운스 3.2배 확대 + 역투영 코트 라인 `aiInv3`/`aiCourtSegs` + 궤적·착지점, 미니 코트 낙하점 `aiMiniCourt`). 카드는 모든 정밀 추적 결과에 붙음. 테스트 `deploy/tests/challenge_test.mjs`** | 실기기(폴드3 캠) 확인 |
| 랠리 자동 클립 | ✅ | 점수 탭 = 직전 랠리 | 타구음 자동 감지 |
| 자동 점수 기록 | 🟡 | 리모컨·워치 브라우저 입력 | 영상만으로 자동 점수 |
| 샷 통계 | 🟡 | **주의(9/22 검증): 스핀 분류 `aiSpin`과 속도 `clShotInfo`는 신뢰 불가 — 9/18 139바운스에서 슬라이스 0개·탑스핀 55%(화면 각도를 분류할 뿐), 속도는 샷별 편차 큼. 물리 기반 `research/pipeline/traj3d.py`(3차원 탄도 맞춤)는 합성에선 정확하나 실영상은 신뢰 통과 11%. 하이라이트에서는 기본 제외함. 앱 표시는 사용자 결정 대기.** 스핀 분류, 코트 커버리지, **라이브 샷 정보(스핀·속도·코스·깊이, v0.64)** + **v0.78 화면 구석 구질 패널·샷 꼬리(캠/뷰어), 속도는 바운스 인접 구간 중앙값(공중 점 사상으로 200km/h 나오던 오류 수정, 15~170 범위만)** | 실코트 튜닝, 낙하 위치 히트맵, rpm·포핸드/백핸드는 불가 |
| 폼 분석 | ✅ | MoveNet 스켈레톤·관절 각도 | — |
| 워치 연동 | 🟡 | 갤럭시워치 브라우저 r.html | 애플워치 네이티브(Phase 3) |

## 7. 다음 할 일 (2026-09-03 기준 우선순위)
1. [사용자] **v0.49 실기기 QA** — 공기계에서 📡 클럽캠 → ✨ 코트 자동 보정(주황 윤곽 확인) → 🤖 라이브 판정 켜기 → 공 튀기면 내 폰 뷰어에 🟢/🔴 피드 뜨는지, 아슬아슬 시 자동 클립이 판독 센터에 열리는지, 폰 발열. 그리고 v0.47 항목(홈 바로가기 3개, 6자리 코드, 단식 토글, 실명 통일 체크박스)
1-2. [사용자] 클럽캠 v0.45 실기기 테스트(폰+공기계)
2. [사용자] 주말 실코트 영상 — 폰 1080p 60fps, 베이스라인 뒤 코너 2m, 앱 📐설치 가이드 구도
3. [사용자] 코트 기둥 사진 → 펜스 거치대 추천
4. **자료 수집 단계(2026-09-08~)** — 사용자가 초광각 렌즈+아이폰 11로 1080p60 촬영해 `training-data/raw/<날짜_라벨>/`에 계속 넣기로(`training-data/촬영가이드.md`). 첫 클립으로 초광각 왜곡 확인 → 필요 시 렌즈 보정 추가. **학습 파이프라인 완성(2026-09-08)** — `research/RUNBOOK.md` 순서대로: 영상이 `training-data/raw/<날짜_라벨>/`에 들어오면 label_all → review_sheet → build_dataset → train(--arch wasb 파인튜닝 | tiny 증류) → eval(results/<날짜>/eval_*.md) → export_onnx → app/ 교체. **원클릭 = 루트 `학습_돌리기.bat`(`research/run_all.ps1`, 2026-09-09)** — 새 영상만 라벨하고(라벨은 `label_all.py --shard k/4` 4병렬이 4배 빠름) 이전 버전에서 이어 tiny-xl v(N+1) 학습(-Limit 16000 -Epochs 12 기본) 후 v1·WASB와 비교표 출력, ONNX까지. **실적: 2026-09-09 경기 120개(2.1GB) → v2 → 앱 v0.99, 2026-09-10 밤샘 v3(32k×16에폭 231분) → 앱 v1.02, **2026-09-13 경기 130개(캠전체 120+클럽캠 10, 07:15~09:36) → v4(48k×12에폭 359분, 낮엔 GPU 86°C 열 스로틀로 에폭 50분→밤 24분) → 앱 v1.04, **2026-09-14 실내 경기 59개(120개 중 61개는 폰이 가방에 들어간 검은 화면 → exclude) → v5(64k×10에폭 290분) → 앱 v1.11(9/16), **2026-09-16 실내 경기 181개(캠 폰 240개 중 9/14 검은 중복 58·9/15 1개 제외) → v7(64k×10에폭 5시간) → 앱 v1.12(9/17 00:08), **2026-09-18 실내 경기 185개 → v8(64k×10, 9/18 21:10~9/19 02:05) = v7과 동급(검증 8개 clean 89.9/91.5/83 vs 90.1/91.8/83) → 미채택**. 두 번 연속 제자리 = WASB 의사 라벨 포화 확정 — 손 정답·두 캠 교차 라벨·다른 코트 없이는 학습 무의미(`research/results/2026-09-19/README.md`). **9/22: 아이폰 낮(높은 카메라, 공 검출 36%) 98개 → v9(v7 이어 64k×10, 18:01~) + 야간 다른 코트(아이폰야간 120·폴드3야간 114, 폴더명 공백 제거) → v10(v9 이어) 체인 — 폴드3 낮 92개는 초광각(9.9%)이라 exclude. 체인·평가·버전업 절차는 인수인계 2-33.** **결과(9/24): 새 코트(파란 하드코트) 139개 → v11 = v10과 동급(검증 12개 90.5/92.5/119 vs 90.5/92.3/122) → 미채택. v10이 이미 처음 보는 이 코트에서 93.5/95.0 — 이득은 모델이 못 하는 조건에서만(`results/2026-09-24`). 체인 스크립트 중지는 스크립트 bash.exe까지 확인해 종료할 것(TaskStop만으론 살아남아 학습이 겹칠 뻔함).** **결과(9/23): v10 채택 → 앱 v1.24. 이 PC의 GPU는 AutoShorts 예약 작업(01:30 hq_fill·04:00 hist_produce, Flux 7~11GB)과 공유라 겹치면 OOM — 학습 전 `Get-ScheduledTask *AutoShorts*` 확인, 05~09시 권장, 죽으면 `last.pt`에서 남은 에폭만 이어 감(`scratchpad/resume_v10.sh` 방식). 학습 중엔 GPU 16GB를 다 써 라벨 동시 실행 불가.** `label_all.py --only`는 경로 부분 문자열(키가 아님 — 파일명으로 줄 것). **테스트 스크립트는 `deploy/tests/`에 보관**(`%TEMP%\pwtest`는 임시 폴더 정리로 smoke_review·wasb_e2e가 사라져 재작성; playwright-core만 pwtest에 두고 `createRequire`로 부름). 학습 stdout은 블록 버퍼링이라 진행은 `ckpt/<name>/log.csv`로**(`research/results/2026-09-09/README.md`). 캠 폰 영상은 v0.98 [PC로 영상 보내기 대기]로 PC 크롬에 직접 수신(실기기 검증됨). WASB는 v0.79부터 판독 센터에 탑재됨(사용자 결정). 사용자는 1080p60 영상을 계속 제공하기로. 목표: 프레임·바운스 재현율 95%, 소형 모델 폴드3 WebGPU 15fps
5. 미니캠 SD카드 3장 추가(64~128GB) 후 인아웃 증거용 배치
6. **점검(2026-09-03)에서 남긴 것**: 서비스 박스·네트 판정면, 30초 스캔 seek 376회 → 성능·중단 버튼, TrackNet 실패 시 자체 추적 폴백, 미니게임 3종 코드 3벌 통합(저장·순위·점수 검증), 클럽 랭킹(참가/승리 포인트) vs 홈 내 랭킹(ELO) 산식 불일치 안내, 손떨림 경고가 aiFinish에 덮이는 문제, innerHTML 이스케이프 전수
7. 후보: KDK 13~16명 대진표, 월간/시즌 랭킹, 오프라인 경기 서버 ELO 반영 규칙, 온보딩 단순화, 아이폰 사파리 QA

## 8. 상용화 체크리스트 (2026-09-06 정리 — 우선순위 순)
**A. 법·계정 (없으면 공개 못 함)**
1. 개인정보처리방침·이용약관 페이지 + 가입 시 동의 체크(현재 없음). 영상·위치·실명이 들어가므로 필수. 촬영 동의 문구(클럽캠은 타인이 찍힘).
2. 이메일 인증 다시 켜기(베타용 OFF 상태) + 비밀번호 재설정 검증 + 카카오 로그인.
3. 계정 삭제 시 storage 클립·학습 샘플·kdk_events 개인정보까지 삭제(현재 soft_delete는 프로필만).
**B. 보안 (베타 확장 전)**
4. `score:` 채널 공개 → 워치 리모컨에 짧은 토큰 발급해 private 채널로.
5. innerHTML 이스케이프 전수(141곳 중 4곳만 esc) — 서버 닉네임·클럽명·게시글.
6. 학습 샘플·클립 storage에 사용자별 경로 정책(지금은 authenticated 전체 조회 가능) + 보관 기간(30일) 자동 삭제.
7. Rate limit: kdk_events insert·storage upload에 per-user 제한(무료 한도 보호).
**C. 서비스 안정성**
8. Supabase 무료 → Pro(월 25달러): storage 1GB·Realtime 동시접속·일시정지 방지. 트래픽 알림.
9. 오프라인/재접속 처리(핫스팟 끊김 시 뷰어·리모컨 자동 재연결 UI), 에러 로깅(Sentry 등) — 지금은 콘솔뿐.
10. 브라우저 매트릭스: 아이폰 사파리(PWA 카메라 불가 안내 화면), 갤럭시 크롬, 워치. 자동 업데이트 강제(구버전 차단).
**D. 제품**
11. 온보딩 단순화 + 클럽 초대 링크(코드/QR)로 가입 → 자동 클럽 멤버.
12. 클럽장 권한 UI(멤버 관리·기록 정정 — 007 정책은 있음, 화면 없음), 클럽별 규칙 설정(노애드·게임 수·포인트 배점).
13. 라이브 스코어 ↔ KDK 보드 자동 연동, 대회 모드(드로우·코트 배정·결과 자동 집계) — 최종 목표.
14. 요금: 클럽 단위 구독(클럽장 결제) 검토 — 개인 결제보다 단순. 결제 전까지는 무료 클럽 3~5곳 확장.
**E. 코드 부채**
15. 단일 HTML 542KB → 모듈 분리·빌드(Phase 3와 함께), 미니게임 3종 코드 통합, 테스트 자동화(현재 스모크 스크립트 수동).

## 9. 관련 문서
- **저장소(GitHub)로 연 세션용 사본**: `pages-repo/sync/`에 인수인계·CLAUDE.md·최신 세션기록을 이메일 제거 후 복사(9/23, `deploy/sync-docs.ps1` — 문서 갱신 뒤 실행하면 커밋·푸시까지). 원본은 이 PC. 폰에서 이어 가려면 원격 제어 → Tennis PC 세션이 정석(로컬 파일·research·테스트 전부 있음).
- **`인수인계_다음세션.md` — 새 세션이 가장 먼저 읽을 요약**(현재 버전·최근 작업·다음 할 일·규칙 요약·아티팩트 URL). 세션 끝날 때마다 갱신할 것.
- `세션기록_YYYY-MM-DD.md` — 그날 작업 인수인계(최신본 먼저). 폰용 페이지는 `deploy/build-sessions.mjs`로 생성(md → HTML, 스크래치패드에 `tennismate-sessions.html`) 후 아티팩트 "테니스메이트 세션기록"에 같은 URL로 재발행(메모리 `tennismate-status-artifact` 참고)
- `claude/인수인계-ClaudeCode.md` — 08-30 구버전(v0.38 기준), Phase 2 설계 배경 참고용. (루트의 handoff.md는 중복이라 2026-09-03 삭제)
- `pages-repo/sync/메모.md` — 09-01 인수인계
- `클럽캠/사용법.md` — 미니캠 운영법
- 메모리: `tennismate-phase2-state.md`, `supabase-cli-on-this-pc.md`
